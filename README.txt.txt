Mudar un equipo de ciudad:
{{host}}/api/v1/teams/{teamId}/moveon?newcity={cityName}

Simular un partido:
winagainst
{{host}}/api/v1/teams/{winnerTeamId}/winagainst?rivalid={loserTeamId}
un equipo no puede enfrentarse a si mismo